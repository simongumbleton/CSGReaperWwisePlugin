Wwise transfer:
- originals matches wise should match existing file original locations if exists
- templates?
- expose more properties like loop
- customise event location



Versioning can we add things as new sources? 
What is the versioning token?

Better way to choose import location
Load the originals location from config file - file browser?
Make apply button BIGGER!


Move create component into its own tab/window?


Render que open bypassed plugins in disabled state?






Region metadata:
- Needs a refresh button
- Need to read the current project regions and merge/sync with the ExtState values before displaying/populating the UI
- UI allign to grid
- Contain UI in scrollable window/layer